import 'package:flutter/material.dart';

class L2LFeed extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      "L2L Feed",
      style: TextStyle(fontSize: 40),
    ));
  }
}
