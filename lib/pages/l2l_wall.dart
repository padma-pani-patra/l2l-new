import 'package:flutter/material.dart';

class L2LWall extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      "L2L Wall",
      style: TextStyle(fontSize: 40),
    ));
  }
}
