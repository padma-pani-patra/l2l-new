import 'package:flutter/material.dart';

class MyLesson2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      "My Lesson2",
      style: TextStyle(fontSize: 40),
    ));
  }
}
